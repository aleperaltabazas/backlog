# Skip empty columns

If WIP column is empty, moving from backlog to done column should be in a single stroke instead of passing by WIP (and nothing being selected, it's bad UX because we lose the cursor)
